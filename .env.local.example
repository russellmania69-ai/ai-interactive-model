# Local environment variables for development
# Copy this file to `.env.local` and fill in your Supabase project values.

VITE_SUPABASE_URL="https://your-project.supabase.co"
VITE_SUPABASE_ANON_KEY="your-anon-public-anon-key"

# Optional: override the functions URL or other settings if needed
# VITE_API_BASE_URL="http://localhost:54321"

# When developing without a Supabase project, set this to `true` to use a local
# mock implementation of Supabase (not for production):
# VITE_USE_SUPABASE_MOCK=true
