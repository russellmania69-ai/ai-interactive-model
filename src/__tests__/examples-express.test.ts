// Moved to examples/__tests__/examples-express.test.ts
// This placeholder prevents the examples tests from running under `src/**` patterns.

import { describe } from 'vitest'

describe.skip('examples tests moved', () => {})

