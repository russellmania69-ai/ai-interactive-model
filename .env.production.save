# install Homebrew if you don't have it (skip if you 
do)
# /bin/bash -c "$(curl -fsSL 
https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# then install gh
brew install gh

# Production environment variables for Vite (build-time)
# Copy values from your production Supabase/Stripe/PayPal accounts.
# After creating/updating this file run `npm run build` and upload the generated
# `dist/` contents to your GoDaddy `/public_html` root.

VITE_APP_ENV=production
# Supabase
VITE_SUPABASE_URL=https://your-supabase-project.supabase.co
VITE_SUPABASE_KEY=public-anon-or-service-role-key

# Payment providers (public keys only)
VITE_STRIPE_PUBLIC_KEY=pk_live_...
VITE_PAYPAL_CLIENT_ID=your-paypal-client-id

# Optional analytics/monitoring keys
VITE_SENTRY_DSN=

# NOTE: These VITE_ variables are baked into the build at compile-time.
# If you change values, rebuild (`npm run build`) and re-upload `dist/`.


# open in nano (or use your editor)
nano .env.production
# Add or replace these two lines:
# VITE_SUPABASE_URL=https://your-project-ref.supabase.co
# VITE_SUPABASE_KEY=eyJ...your-anon-key...

^X


X


